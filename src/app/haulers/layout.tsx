export const metadata = {
  title: "Deliver with CropFresh | Hauler Partner Program",
  description: "Join CropFresh as a hauler and earn more with optimized routes, instant UPI payments, and flexible schedules. No empty returns.",
};

export default function HaulersLayout({ children }: { children: React.ReactNode }) {
  return children;
}
